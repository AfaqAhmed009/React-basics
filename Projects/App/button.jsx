function Button(){
   
    Text = "Search"
    // Hwew we are returning the jsx function
    return (

        // Frangments
        <>
          <button type="submit">
            {Text}
          </button>
        </>
    );
}

export default Button;